# project-microservice-backend
This project is a **backend microservices-based user management system** built with **Spring Boot**. It leverages **Spring Security**, **REST APIs**, **JWT** for authentication, **Feign** for inter-service communication, **JPA** for persistence, **Junit &amp; Mockito** for testing, and **Docker** for containerization.
